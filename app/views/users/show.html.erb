This is the profile of <%= @user.name %>

<br>
"<%= @user.latest_status_content %>" <%= time_ago %>

<br>

<%= form_for Status.new, remote: false do |f| %>
  <%= f.text_field :content, placeholder: 'What are you doing?', id: 'update-status' %>
  <%= f.submit 'Submit' %>
<% end if @user == current_user %>