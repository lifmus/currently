<div class='container'>
  <h1>This is the profile of <%= @user.name %><%= link_to 'Unfollow', @connection, method: :delete, class: 'btn btn-danger' if @connection %></h1>

  <h3>"<%= @user.latest_status_content %>" <%= time_ago(@user.latest_status) %></h3>

  <br>

  <% if @user == current_user %>
    <%= render partial: 'shared/update_status' %>
  <% end %>  
</div>